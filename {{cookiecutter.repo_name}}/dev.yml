django:
  build: .
  env_file: .env
  ports:
    - "8000:8000"
  links:
    - postgres
  volumes:
    - .:/usr/src/app
  command: manage.py runserver 0.0.0.0:8000

postgres:
  image: postgres:9.4
  ports:
    - "5432:5432"
